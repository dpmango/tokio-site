include ../shared/_mixins

// HEADER :: START

mixin header(absoluteLinks)
  - absoluteLinks = absoluteLinks || false

  header.b-header.root-block#header
    .container.layout__offset.b-header__wrapper
        nav.b-header__nav
          -
            var navigation = [
            ['Меню', 'menu'],
            ['О нас', 'about'],
            ['Новости', 'news'],
            ['Контакты', 'contacts'],
            ]
          each navItem in navigation
            +anchor(
              href='index.html#' + navItem[1]).size-smallest.b-header__nav_item= navItem[0]
        +anchor.hamburger-btn.hidden
        .b-header__logo
          +anchor(href='index.html#home')
            .icon-logo.b-normal
            .icon-tokio-icon.b-tab.b-mob
        .b-header__reserve
          +anchor(js-modal-invoker="modal-reserve")
            .icon-reserve.b-normal
            .icon-reserve-white.b-tab
            .icon-reserve-mobile.b-mob

// HEADER :: END
